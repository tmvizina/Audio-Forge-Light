# CLAUDE.md

This project keeps one canonical agent document: **[AGENTS.md](AGENTS.md)**.

Read it before touching anything — it holds the repo map, the commands, the
Python↔Node contract, and the invariants that must not be "improved" away.
Everything that would otherwise live here lives there instead, so the two never
drift apart. If you learn something an agent needs next session, add it to
`AGENTS.md`, not to this file.
